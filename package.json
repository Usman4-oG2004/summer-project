{
  "name": "summer-project",
  "version": "1.0.0",
  "scripts": {
    "lint:html": "htmlhint \"**/*.html\"",
    "lint:css": "stylelint \"**/*.css\"",
    "lint": "npm run lint:html && npm run lint:css",
    "build": "mkdir -p dist && cp -r src/* styles/ dist/ 2>/dev/null || cp src/*.html dist/ 2>/dev/null || true"
  },
  "devDependencies": {
    "htmlhint": "^1.1.4",
    "stylelint": "^16.2.1",
    "stylelint-config-standard": "^36.0.0"
  }
}
