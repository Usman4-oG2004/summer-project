{
  "name": "summer-project",
  "version": "1.0.0",
  "scripts": {
    "lint:html": "htmlhint \"**/*.html\"",
    "lint:css": "stylelint \"**/*.css\"",
    "lint": "npm run lint:html && npm run lint:css",
    "build": "node -e \"const fs=require('fs'); fs.mkdirSync('dist', {recursive: true}); fs.cpSync('src', 'dist', {recursive: true}); if (fs.existsSync('styles')) fs.cpSync('styles', 'dist', {recursive: true});\""
  },
  "devDependencies": {
    "htmlhint": "^1.1.4",
    "stylelint": "^16.2.1",
    "stylelint-config-standard": "^36.0.0"
  }
}
